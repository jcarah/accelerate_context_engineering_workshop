[project]
name = "evaluation"
version = "0.1.0"
description = "Evaluation scripts for ADK agents"
requires-python = ">=3.10"
dependencies = [
    "pandas>=2.0.0",
    "requests>=2.31.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Tool Interactions Schema",
  "description": "Schema for the list of tool interactions extracted from the session events.",
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "tool_name": {
        "type": "string",
        "description": "The name of the tool called."
      },
      "input_arguments": {
        "type": ["object", "null"],
        "description": "The arguments passed to the tool."
      },
      "output_result": {
        "type": ["object", "string", "number", "boolean", "null"],
        "description": "The result returned by the tool."
      },
      "call_id": {
        "type": "string",
        "description": "Unique identifier for the tool call."
      },
      "status": {
        "type": "string",
        "description": "Status of the tool call (e.g., 'completed', 'no_response').",
        "enum": ["completed", "no_response"]
      }
    },
    "required": ["tool_name", "call_id"]
  }
}
